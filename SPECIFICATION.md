# ニコ生サイドバー 仕様書

| 項目 | 内容 |
|---|---|
| 対象 | Chrome 拡張「ニコ生サイドバー」 |
| バージョン | 1.10.8（`manifest.json`） |
| 作成日 | 2026-07-29 |
| 基準コミット | `a9db630` |
| 記述の根拠 | `src/` 配下の実装コードを読み取って作成 |

---

## 0. この文書について

### 0.1 目的

本書は、本拡張が **何を・どういう条件で・どう処理するか** を実装レベルで確定させた仕様書である。改修時の判断基準、および挙動が仕様か不具合かの切り分けに用いる。

### 0.2 位置づけと読み分け

| 文書 | 役割 |
|---|---|
| **本書** | 機能ごとの確定仕様（What / When / How） |
| `doc/01`〜`doc/08` | 設計背景・モジュール詳細・データフロー |
| `doc/09-gotchas-and-techdebt.md` | 落とし穴・技術的負債・過去の失敗と対策（**改修前に必読**） |
| `doc/10-verification-playbook.md` | 検証手順と自動検証の使い方 |

### 0.3 記述の信頼度について

本書は実装コードの読み取りに基づく。ただし **全機能を実機で動作確認したわけではない**。実測・実機確認を伴う記述には「実測」と明記する。それ以外は「コードがそう書かれている」という意味である。

> ⚠️ `doc/` は過去に実装から乖離した実績がある（撤去済み機能が章ごと残存していた）。本書と実装が食い違う場合は **実装を正とし、本書を修正すること**。

---

## 1. 概要

### 1.1 目的

ニコニコ生放送の視聴ページに常設サイドバーを設置し、**フォロー中の放送中番組の一覧をリアルタイムに表示する**。視聴を中断せずに他番組の状況を把握し、任意の番組へ遷移できるようにする。

### 1.2 動作条件

| 項目 | 値 |
|---|---|
| プラットフォーム | Chrome 系ブラウザ（Manifest V3） |
| 適用URL | `https://live.nicovideo.jp/watch/*` |
| 注入タイミング | `document_start` |
| 必要権限 | `storage` のみ |
| 前提 | niconico へのログイン（未ログイン時はリスト取得不可） |

### 1.3 起動しない条件

以下のいずれかに該当する場合、拡張は初期化を行わず即座に終了する。

| ID | 条件 | 理由 |
|---|---|---|
| SKIP-1 | URL に `?popup=on` が含まれる | 姉妹拡張「別窓くん」のポップアップ内での二重起動を防止 |
| SKIP-2 | `#root` 要素が存在しない | ニコ生の想定DOM構造でない（仕様変更・別ページ） |
| SKIP-3 | 既に初期化済み（`isSetupCompleted`） | 二重初期化の防止 |

### 1.4 用語

| 用語 | 定義 |
|---|---|
| **リスト** | どの番組が放送中かの一覧。フォローAPI と notifybox の**和集合** |
| **詳細** | 視聴者数・コメント数・サムネURL・配信者名・開始時刻など。同じフォローAPI から同時に得る |
| **ライブサムネ** | 配信画面のスクリーンショット。同一URLのまま内容が時間変化する |
| **固定画像** | 配信者が設定した静止画。ライブサムネではない |
| **カード** | 1番組に対応するサイドバー内のDOM要素（`.program_container`） |
| **常設ループ** | ページ滞在中に1本だけ動き続けるリスト更新タイマー |
| **自己連鎖タイマー** | 「処理完了後に次を予約する」方式のタイマー。周期が作業時間ぶん伸びる |
| **ローディングセッション** | 更新処理の実行中を表す状態。更新ボタンの無効化に使う |

---

## 2. システム構成

### 2.1 レイヤ構成

```
main.js（エントリ：初期化・イベント配線・各層への委譲）
  ├─ core/AppState.js          全状態の集約と一括クリーンアップ
  ├─ managers/                 UpdateManager / LoadingManager / AutoNextManager
  ├─ render/                   sidebar.js（DOM生成・サムネ更新）/ animatedThumbnail.js
  ├─ ui/                       layout.js（幅調整）/ sidebarControl.js（開閉・ドラッグ）
  ├─ handlers/optionsHandler.js 設定フォーム
  ├─ services/                 api.js / followPageSource.js / storage.js / status.js / animFrameStore.js
  ├─ utils/                    dom.js / error.js / sorting.js
  └─ config/constants.js       全定数
```

### 2.2 循環依存の回避

`AutoNextManager` はリスト更新を必要とするが、`UpdateManager` を直接 import すると循環する。**`main.js` が `startWatcher(updateSidebar)` として関数を注入する**方式で回避している。

### 2.3 ライフサイクル

| フェーズ | 契機 | 回数 |
|---|---|---|
| モジュール評価 | スクリプト注入時 | 1回 |
| 初期化（`setup`） | `DOMContentLoaded` | **1回のみ** |
| 後始末（`cleanup`） | `beforeunload` / `pagehide` | 1回 |

> **SPA的な再初期化経路は存在しない。** 番組間の移動も `location.assign()` による完全遷移であり、その都度ページが読み込み直される。

> ⚠️ `beforeunload` / `pagehide` は **ページが破棄されずに生き残る場合がある**（bfcache 復帰、遷移キャンセル）。`cleanup` はこの場合も走るため、後始末は「復帰不能な破棄」にしてはならない。

---

## 3. データ仕様

### 3.1 外部API

#### API-1: 番組詳細＋リスト（フォロー中ページのフロントAPI）

| 項目 | 内容 |
|---|---|
| URL | `https://live.nicovideo.jp/front/api/pages/follow/v1/programs?status=onair&offset=<N>&limit=100` |
| 認証 | `credentials: 'include'`（**ログイン Cookie 必須**） |
| 応答 | `{ data: { programs: [...], total: N } }` |
| `offset` | **0始まりのページ番号**（件数オフセットではない）。ページNは全体の `[N*100, N*100+100)` |
| ページング | `total` に達するまでループ。id で重複排除 |
| 安全上限 | `MAX_PAGES = 5`（最大500件） |
| 返り値 | **失敗時 `null` / 成功時は配列（0件でも配列）** ＝「取得失敗」と「放送中0件」を区別できる |

**並び順(beginAt)・詳細・100件を超える番組はこの1本から得る。** 新着検知の速さは API-1b が補う。

#### API-1b: 番組リスト（notifybox）— 新着検知の速さ担当

| 項目 | 内容 |
|---|---|
| URL | `https://papi.live.nicovideo.jp/api/relive/notifybox.content.php?rows=100` |
| 認証 | `credentials: 'include'`（**ログイン Cookie 必須**） |
| 応答 | `{ meta: { status: 200 }, data: { notifybox_content: [...] } }` |
| 要素 | `{ id: "<lv抜きの数値>", title }` 程度。**詳細は含まない** |
| 上限 | **`rows=100`・ページングなし** |
| 返り値 | 失敗時 `false` |

**user番組の新着検知が API-1 より 20〜101秒 速い**（2026-07-29 実測・9件）。この1点のために併用する。

#### API-1 と API-1b の合成

両方を並列に取得し、**和集合**を表示する（`_mergeSources`）。詳細の優先順位は
**API-1 の実データ ＞ storage の前回値 ＞ API-1b の最小情報**。

API-1b にしか無い番組は「たった今始まった新着」なので、`beginAt` 不明のまま新着順の先頭へ置き、
次の周期で API-1 の実データに置き換わる。

> ⚠️ **notifybox を「絞り込み」に使ってはならない。** 旧実装は notifybox に載った番組だけカード化しており、`rows=100` のため**放送中が100件を超えると101件目以降が表示されなかった**（詳細は500件まで取得して捨てていた）。和集合にすることでこれが解消している。

#### API-2: 番組詳細（個別・サムネ補完専用）

| 項目 | 内容 |
|---|---|
| URL | `https://api.cas.nicovideo.jp/v1/services/live/programs/lv{id}` |
| 認証 | **なし**（Cookie を送らない） |
| 用途 | API-1 でライブサムネが取れなかった番組**のみ**を補完 |
| 上限 | 1サイクルあたり `MAX_DETAIL_FALLBACK = 30` 件 |

> 旧方式（全番組を API-2 で個別取得＋レート制限キュー）は撤去済み。復活させないこと。

### 3.2 内部データモデル：`programInfo`

API-1 の各要素を `mapApiProgramToInfo()` が以下へ変換する。

| フィールド | 型 | 由来・規則 |
|---|---|---|
| `id` | string | `"lv..."` 形式 |
| `title` | string | 空なら `'タイトル不明'` |
| `providerType` | string | API の `community` → `'user'` に正規化 |
| `contentOwner` | object | `{ id, name, icon }` |
| `liveScreenshotThumbnailUrls.middle` | string? | **ライブスクショと判定できた場合のみ**設定 |
| `large1280x720ThumbnailUrl` | string? | 同上 |
| `thumbnailUrl` | string | 表示用。user はライブスクショのみ、channel/official は `listingThumbnail` をそのまま |
| `isMemberOnly` | boolean | `isFollowerOnly` |
| `viewers` | number | `statistics.watchCount` |
| `comments` | number | `statistics.commentCount` |
| `onAirTime.beginAt` | string? | msエポック → ISO文字列 |
| `_fetchedAt` | number | 保存時に付与 |

#### ライブスクショ判定（`isLiveScreenshotUrl`）

```
URL に "/screenshot/" を含む、または ホストが dlive.nicovideo.jp
```

| provider | 表示用 `thumbnailUrl` | 定期更新の対象 |
|---|---|---|
| user | ライブスクショのみ（固定画像は表示しない） | ライブスクショのみ |
| channel / official | `listingThumbnail` をそのまま（固定画像でも表示） | **ライブスクショのみ** |

> **前提（ニコ生の仕様・利用者確認済み）**: チャンネル番組にライブサムネは提供されない。チャンネルのサムネが動かないのは**不具合ではない**。これを「直そう」としないこと。

### 3.3 永続化

| 保存先 | キー | 内容 |
|---|---|---|
| `chrome.storage.local` | 各設定キー（§7） | 設定値。**全タブで共有され、変更は onChanged で伝播** |
| `localStorage` | `programInfos` | 番組詳細の配列。上限 `maxSaveProgramInfos = 200` |
| IndexedDB | 動くサムネのフレーム | 番組IDキー。上限300件・TTL30分 |

`programInfos` の上限トリムは、**更新されたレコードを末尾へ移動**してから先頭を落とす。これにより「今回更新されなかった＝放送終了済み」から優先的に削除される。

---

## 4. 機能仕様

### F-01. サイドバーの設置

| 項目 | 内容 |
|---|---|
| 契機 | `setup()` 内 |
| 処理 | `body` を `display:flex`、`#root` を `flex-grow:1` に変更し、`body` 先頭にサイドバーDOMを挿入 |
| 追従 | `window.resize`（30msデバウンス）／`ResizeObserver`×2／シアターボタンclick で幅を再計算 |

**カード列数**は「意図した幅」（`appState.sidebar.width`）で決定する。開閉アニメーション中の実測幅を使うと、列数が1↔多列で往復してサムネが一瞬巨大化する崩れが起きるため。

| サイドバー幅 | 列数 |
|---|---|
| 〜300px | 1 |
| 〜500px | 2 |
| 〜700px | 3 |
| 〜900px | 4 |
| 〜1100px | 5 |
| 〜1300px | 6 |
| 〜1500px | 7 |
| 1500px超 | 8 |

カード幅のみ `width: 100/列数 %` を設定する。サムネ幅は CSS の `.program_thumbnail { width:100% }` に任せる（**サムネに幅を設定しないこと**）。

### F-02. サイドバーの開閉

| 項目 | 内容 |
|---|---|
| 契機 | `#sidebar_button` クリック／初期化時の自動判定／他タブからの伝播 |
| 保存 | `isOpenSidebar`（`chrome.storage.local`） |
| 開いた時 | サムネタイマー起動 → リスト更新の位相リセット → `performManualUpdate()` |
| 閉じた時 | サムネタイマー停止＋自動移動カウントダウン停止。**リスト更新の常設ループは停止しない**（取得のみ見送る） |

> ⚠️ **二重発火の防止**: 自タブでの開閉操作は同期的に反映されるが、`isOpenSidebar` の書き込みで `chrome.storage.onChanged` が**自タブでも**発火する。onChanged 側は「現在の状態と異なる場合のみ処理」するガードを持つ。これを外すと開くたびにリスト取得が2回走る。

> ⚠️ **閉じた時に自動移動のカウントダウンも停止する**のは既存の仕様である。ここを変更すると「サイドバーを閉じたのにページが勝手に遷移する」という新しい挙動になる。

### F-03. 幅のドラッグ変更

| 項目 | 内容 |
|---|---|
| 契機 | `#sidebar_line` の mousedown → mousemove |
| 最小幅 | `sidebarMinWidth = 180`px |
| 保存 | **mouseup 時のみ** `sidebarWidth` を保存（ドラッグ中は保存しない） |

### F-04. テーマ（ライト／ダーク）

| 項目 | 内容 |
|---|---|
| 実装 | CSSカスタムプロパティ（`--sb-*`）を `body`（ダーク）と `body.nicosidebar-light`（ライト）で切替 |
| 既定 | `light` |
| 適用順序 | **サイドバー挿入より前に `applyTheme()` を実行**（初回のちらつき回避） |
| 伝播 | `sidebarTheme` の onChanged で他タブにも反映 |

### F-05. 設定パネル

| 項目 | 内容 |
|---|---|
| 契機 | `#setting_options`（歯車）クリック |
| 表示 | `.sidebar_body` に `.show-settings` を付与し、**番組リストと設定を入れ替え表示**（ポップアップではない） |
| 閉じる | `#settings_close`（×）または Esc |
| 保存 | フォームの `change` イベントで即保存 |

### F-06. リストと詳細の取得（`updateSidebar`）

全ての更新経路（初回・定期・手動・自動移動）が通る中核処理。

**処理順序**

```
1. ローディングセッションを開始（既に動いていれば相乗り）
2. API-1（リスト）と API-2（詳細）を Promise.all で並列取得
3. 詳細を localStorage へ upsert
4. リストと突き合わせてカードを構築
5. 並び替え → 列数調整 → 件数表示
6. サムネタイマーを新規/削除カードに同期
```

**失敗時の扱い**

| 状況 | 挙動 |
|---|---|
| API-1 が失敗（null） | 既存DOMを維持。件数のみ更新。`#api_error` を表示。**フォールバックしない** |
| 和集合が空 | 取得は成功・放送中0件。件数を0に |
| 個別カードの生成失敗 | **その番組だけスキップ**しリスト全体は描画する |

**DOM更新の方針（差分更新）**

- 既存カードは**その場で属性のみ更新**（DOMを動かさない）
- 追加・削除・並び順の変化がある場合**のみ**、`replaceChildren` で組み替える

> DOM組み替え区間（`isInserting = true` 〜 `false`）は **await を挟まない完全な同期処理**である。ここに非同期処理を入れてはならない。

### F-07. リストの定期更新（常設ループ）

| 項目 | 内容 |
|---|---|
| 開始 | `setup()` で1回のみ（`startSidebarLoop`） |
| 停止 | `cleanup()` のみ（`destroySidebarLoop`） |
| 間隔 | `updateProgramsInterval`（60 / 120 / 180秒。既定120） |

**設計原則**

> **ループを作り直さない。** `_sidebarNextDueAt`（次に取得してよい時刻）が唯一の正であり、タイマーは単なる目覚ましである。誰が再スケジュールしても同じ値から遅延を計算するため、複数の呼び出し元が食い違うことがない。

**1サイクルの判定順序**

```
1. 破棄済みか            → 何もしない
2. サイドバーが閉じているか → 何もしない
3. まだ期限前か           → 何もしない
4. 別の更新が進行中か      → 今回は見送り、次周期へ
5. 上記いずれでもない      → updateSidebar() を実行
必ず最後に次の目覚ましを張る（finally）
```

**実効周期**

```
実効周期 = 設定値 + その回の作業時間
作業時間 = max(取得にかかった時間, ローディング最低表示1秒)
```

期限は「**その回が終わった時点**」から数え直す。取得の前に期限を進めると周期が設定値ちょうどに詰まり、外部サービスへの取得頻度が上がる。

> **実測（2026-07-29）**: 設定60秒・取得300ms → 周期 61.02秒 / 61.03秒。60.00秒ちょうどにはならない。

**タブ可視状態による制御は行わない**

サイドバーが開いている間、**裏タブでもリスト取得を継続する**。これは意図的な決定であり（可視ガードは全撤去済み）、`visibilitychange` ハンドラは存在しない。`document.hidden` を参照するのは**サムネ更新側のみ**である。

> ⚠️ ループのタイマーハンドルを `AppState.timers` に置いてはならない。閉じる処理やクリーンアップから外部に破棄され、閉じた状態で起動した場合にループが即死して復活不能になる（例外もログも出ない）。

### F-08. サムネイルの定期更新

| 項目 | 内容 |
|---|---|
| 方式 | **常設ループ1本＋番組ごとの「次に更新してよい時刻」** |
| 基準間隔 | `updateThumbnailInterval = 20秒`（固定・設定不可） |
| 実効周期 | 20秒 + その回の作業時間（番組ごとに独立） |
| 1 tick の処理量 | **1番組だけ**（1件のハングが他を巻き添えにしないため） |

**キャッシュバスター**

ライブサムネは**同一URLのまま内容が変化する**ため、URL末尾に現在時刻を付与して再取得する。

```
…/screenshot.jpg?cache=<epoch ms>
```

既存クエリがある場合は `&` で連結する（`?` の二重付与を避ける）。

**ドリフト（意図的な設計）**

> 起点となる要望: 「リストが一斉に切り替わるのが気持ち悪い。各番組がバラバラのタイミングで更新されてほしい」

「20秒ごと」ではなく「**更新完了後に20秒**」とすることで、作業時間の差により時間とともに位相が自然にばらける。

**成立の必須前提**: 初回サイクルの位相を周期内へ均等配置すること。全カードに同じ初回遅延を張ると原理的にドリフトしない（同時取得により全番組の作業時間が揃い、一斉状態が自己維持される）。位相分散は**必ず基準間隔ぶん後ろへ倒してから**行う（前倒しすると手動更新の一斉更新中に発火し二重取得になる）。

> **実測（2026-07-26）**: 14番組・4分間で周期20.0秒、位相の散らばり18.1〜18.5秒、同時実行は常に1本。

**安定化の仕組み**

| 機構 | 値 | 目的 |
|---|---|---|
| TTL | 10秒 | 成功直後の再取得を抑止（フリッカー防止） |
| エラーバックオフ | 2秒 → 最大60秒（指数） | 失敗URLを叩き続けない |
| プリロード | — | **新画像の読み込み成功後に差し替える**（失敗時に画面が崩れない） |

**空サムネの追撃**

放送開始から3分以内（`newProgramFastPollMs`）の user 番組でライブサムネが空の場合のみ、各サイクルで API-3 を1回叩いて補完する。3分を超えた空サムネは「固定画像運用」とみなし追撃しない。

**背景タブ**

`document.hidden` の間は画像更新を行わず、次サイクルの予約のみ行う。背景では `requestAnimationFrame` が停止し、完了通知が届かないため。

### F-09. 動くサムネ（β版・既定OFF）

| 項目 | 内容 |
|---|---|
| 動作条件 | 設定ON **かつ** マウスがサムネ画像上にある間のみ |
| 保持枚数 | `animatedThumbnailFrameCount = 5`（リングバッファ） |
| 1コマ表示 | `animatedThumbnailPlayIntervalMs = 700ms`（クロスフェード） |
| 再生開始 | 2枚貯まった時点（保持中に2枚目が来れば自動開始） |

**フレームの取得方式（給餌方式）**

> 動くサムネは**自前で定期取得しない**。F-08 の通常サムネ更新が取得した画像を横流しで受け取る。自前でネットワークへ出るのはホバー時の即時取得のみ。

ONの時、F-08 のプリロードは `crossOrigin='anonymous'` で読み込み、成功した画像を動くサムネへ渡す。crossOrigin が失敗した場合は平文で読み直して**表示だけ確保**し、動くサムネへは渡さない（アニメのみ休止・コア表示は無傷）。

**重複排除（知覚ハッシュ）**

```
画像を 16×16 に縮小 → 輝度配列を作成 → 前フレームと比較
  平均差 > 2        → 変化あり
  または 1マスでも差 > 24 → 変化あり（局所変化の検出）
上記のいずれでもなければ破棄
```

ニコ生のサムネ更新間隔が一定でないため、この排除を行わないと同一画像が複数コマ並ぶ。

**永続化**

フレームは IndexedDB に番組IDキーで保存され、リロードや番組移動後に復元される。

| 項目 | 値 |
|---|---|
| TTL | 30分（**最後に異なるフレームが出た時刻**からの経過で判定） |
| 上限 | 300番組 |

TTLの基準が「最後に異なるフレームが出た時刻」なのは、静止しがちな番組がフレーム更新されずに誤って掃除されるのを避けるため。

**共存**

オーバーレイの `z-index` は **1**（姉妹拡張「別窓くん」のホバーボタンより下）。「ベースサムネの上・ホバーボタンの下」を必ず維持すること。

### F-10. 並び順

| 設定値 | 基準 |
|---|---|
| `newest`（既定） | **`beginAt`（放送開始時刻）の降順**。同時刻は lv番号の降順で決定的にする（`data-api-index` 昇順として実装） |
| `active` | 人気度スコア降順 |

**人気度スコア**

```
active-point = (viewers + 1 + comments + 1) ÷ 放送開始からの経過分数
```

経過時間で割ることで「勢い」を評価する（単純な視聴者数だと長時間放送が過度に有利になるため）。

> ⚠️ **新着順に lv番号（ID）を「主キー」として使ってはならない。** lv番号は予約・作成順で採番されるため、予約枠があると放送開始順とずれる。基準は `beginAt` であり、lv番号は同時刻・時刻欠落時の決定的なタイブレークにのみ使う。

並び順の変更は**通信を伴わず**、その場でDOMを並べ替えるのみ。

**整列確定機構は存在しない。** 詳細がリストと同時に揃うため、最初の描画から確定した順序で表示される。

### F-11. オートオープン

| 設定値 | 挙動 |
|---|---|
| `1` | 常に開く |
| `2` | 常に閉じる |
| `3`（既定） | 前回の `isOpenSidebar` を復元 |

判定式: `shouldOpenAtStart = (autoOpen === '1') || (autoOpen === '3' && isOpenSidebar)`

変更は**次回のページ読み込みから**有効。

### F-12. 番組の自動移動（既定OFF）

**処理フロー**

```
1. ニコ生の「番組終了」表示を MutationObserver で検知
2. 最新のリストを取得（古いリストから選ばないため）
3. 現在と異なる番組を先頭から選定
4. モーダル表示＋10秒カウントダウン
5. location.assign() で遷移
```

| 操作 | 結果 |
|---|---|
| キャンセル | 遷移しない |
| プレビューサムネをクリック | カウントダウンを待たず即遷移 |
| サイドバーを閉じる | カウントダウンが停止する |

> ⚠️ **自己駆動ループへの対策**: 終了検知の MutationObserver は `document.body` 全体を監視するため、リスト更新の `replaceChildren` が撒く変異にも反応する。デバウンスがないと「更新→変異→再検知→更新」の無限ループになる。**20秒のスロットル**（`PROGRAM_END_RECHECK_MIN_INTERVAL_MS`）が唯一の抑止機構である。リスト更新の呼び出し頻度を変える改修では、この安全マージンを必ず考慮すること。

### F-13. 手動更新

| 契機 | 更新ボタン `#reload_programs` ／ サイドバーを開いた時 ／ 初回ロード |
|---|---|

**処理順序**

```
1. 多重防止フラグを確認（既に実行中なら何もしない）
2. updateSidebar()
3. 全サムネを強制更新（TTL・バックオフをバイパス）
4. 最低1秒のローディング表示を確保
5. 定期取得の位相をリセット（開いている場合のみ）
```

**ハング対策（2層）**

サムネ反映は `requestAnimationFrame` 駆動のため、背景タブでは完了通知が永久に届かない。対策は2層。

| 層 | 内容 |
|---|---|
| 1 | `document.hidden` なら即座に「完了」として返す（背景では1枚も更新できないため待つ意味がない） |
| 2 | 待ち時間に上限 `manualThumbWaitMaxMs = 30秒` を設ける |

> 1層目だけでは「**待っている最中に背景へ回る**」経路を塞げない。入口ガードのみで満足しないこと。

上限値は、実測の一斉更新（15秒級）を十分に上回り、かつローディングのタイムアウト（60秒）より手前に設定する。

### F-14. ローディングセッション

更新処理の実行中を表す状態。更新ボタンの `.loading` クラスと `pointer-events: none` を制御する。

| 規則 | 内容 |
|---|---|
| 最低表示時間 | 1秒（速すぎると操作の手応えがない） |
| タイムアウト | 60秒（`loadingSessionTimeoutMs`）で強制解除 |
| **相乗り** | 既にセッションが動いている場合、`updateSidebar` は**新しく立てず相乗りする**（`null` を返す） |
| **IDスコープ** | セッションを閉じる側は「自分が始めたID」とだけ照合する |

> ⚠️ セッション開始は前のセッションを終了させずに**上書きする**。素直に新規セッションを立てると動いている処理からロックを奪い、それを閉じると本来の持ち主がまだ実行中なのにボタンが有効化される（＝押せるのに無反応）。相乗りとIDスコープの両方が必要である。

### F-15. エラー表示

| 状況 | 表示 |
|---|---|
| API-1 成功 | `#api_error` を非表示 |
| API-1 失敗 | `#api_error` を表示（ログインリンク付き） |

### F-16. 別窓くん連携

URL に `?popup=on` が含まれる場合、`DOMContentLoaded` ハンドラの先頭で即 `return` する。タイマーは一切起動しない。

### F-17. デバッグ機能

| 関数 | 内容 |
|---|---|
| `window.__testFollowScrape()` | API-2 の取得結果を件数・所要時間・表で表示（**関数名はスクレイプ時代の名残**。実体はフロントAPI） |
| `window.showAnimThumbStats()` | 動くサムネの統計（給餌数・自前取得数・重複破棄・CORS汚染数） |

正常な状態は「給餌数が主・自前取得はホバーのみ・CORS汚染 0」。

---

## 5. タイマー仕様

本拡張は**3系統**のタイマーを持つ。更新ループ2本はどちらも「常設1本＋期限表」の同じモデルである。

| 系統 | 対象 | 本数 | 間隔 | 停止条件 |
|---|---|---|---|---|
| **リスト更新** | 番組一覧・詳細 | 常に1本 | 設定値＋作業時間 | ページ離脱のみ |
| **サムネ更新** | 各カードの画像 | **常に1本** | 20秒＋作業時間（番組ごと） | ページ離脱のみ |
| **自動移動** | カウントダウン | 0〜1本 | 1秒 | キャンセル／サイドバーを閉じる／遷移 |

### 5.1 更新ループ2本に共通の設計原則

| 原則 | 理由 |
|---|---|
| ループを作り直さない | 作り直しの境界で孤児化・二重化が発生するため |
| 次回時刻を唯一の正とする | 複数の呼び出し元が食い違わない |
| 閉じても止めず、取得だけ見送る | 「停止」操作が存在しなければ停止し損なわない |
| 判定に外れても必ず次を予約する | ループが死ぬ経路を破棄のみに限定する |

> **背景**: 旧実装は start/stop/restart でチェーンを生成・破棄していたが、「取得中は保留タイマーが存在しない」ため停止指示が空振りし、(A) 閉じても更新が続く (B) チェーンが二重化する、という欠陥があった。世代トークンで押さえていたが、照合箇所を1つ書き忘れれば穴が開く構造だった。常設ループ化により欠陥の発生源そのものを除去している。

### 5.2 非同期処理を跨ぐ際の注意

> ⚠️ **「タイマーを `clearTimeout` すれば止まる」は `await` を挟む処理では成立しない。** 実行中は保留タイマーが存在しないため空振りする。停止フラグか世代トークンを併用し、**`await` の前後どちらでも照合**すること。

---

## 6. 設定仕様

| 機能 | 保存キー | 値 | 既定 | 反映方法 |
|---|---|---|---|---|
| 表示順序 | `programsSort` | `newest` / `active` | `newest` | 即DOMソート |
| 自動更新間隔 | `updateProgramsInterval` | `60` / `120` / `180` | `120` | 開いていれば位相リセット |
| オートオープン | `autoOpen` | `1` / `2` / `3` | `3` | 次回ロードから |
| 自動移動 | `autoNextProgram` | `on` / `off` | `off` | 監視の開始／停止 |
| 動くサムネ | `animatedThumbnail` | `on` / `off` | `off` | 有効／無効の切替 |
| テーマ | `sidebarTheme` | `dark` / `light` | `light` | body クラスの切替 |
| サイドバー幅 | `sidebarWidth` | 数値 | `360` | フォーム外（ドラッグ） |
| 開閉状態 | `isOpenSidebar` | boolean | `false` | フォーム外（ボタン） |

### 6.1 保存の書き手を分ける規則

`isOpenSidebar` と `sidebarWidth` は **永続化される**（開閉状態は「前回を記憶」するオートオープンに必要）。規則は「保存するかどうか」ではなく「**どの経路が書くか**」である。

| キー | 唯一の書き手 | 契機 |
|---|---|---|
| `isOpenSidebar` | `setIsOpenSidebar()` | 開閉ボタンの操作 |
| `sidebarWidth` | `setSidebarWidth()` | 幅ドラッグの mouseup |
| それ以外の設定 | `saveOptions()` | 設定フォームの変更 |

> ⚠️ **`saveOptions` は `isOpenSidebar` / `sidebarWidth` を書き込んではならない。**
>
> 設定フォームの保存処理は設定オブジェクト**全体**を渡してくる。素直に全キーを書き込むと、**設定を1つ変えただけで自タブのUI状態が全タブへ配信される**。
>
> **具体的な破れ方**: オートオープン `'1'` で開いたタブは、メモリ上の `options.isOpenSidebar` を `true` にするが **storage には書かない**（`main.js` の初期開閉分岐）。この状態でテーマや並び順を変えると `false → true` の変化として storage に書かれ、`chrome.storage.onChanged` が全タブで発火する。**サイドバーを閉じている別タブがこれを「他タブが開いた」と解釈し、幅0のまま（見た目は閉じたまま）リスト取得を開始する。** 「閉じているタブでは取得しない」という不変条件が、更新間隔以外の設定変更で破れていた。
>
> 呼び出し側は設定オブジェクトをそのまま渡してくるため、**除外は保存層（`storage.js`）で行う**こと。呼び出し側の作法に依存させない。

### 6.2 設定変更の伝播

`chrome.storage.local` は**全タブで共有**される。設定変更のハンドラは、サイドバーを閉じている別タブでも発火する。**開閉状態を見ずに処理してはならない。**

---

## 7. 非機能仕様

### 7.1 ネットワーク負荷

| 経路 | 頻度 | リクエスト数 |
|---|---|---|
| リスト＋詳細 | 設定値（既定120秒）ごと | **2**（100件超ならページング分だけ増える） |
| サムネ | 20秒ごと × 番組数 | 番組数ぶん |
| サムネ補完 | 空サムネの番組のみ | 通常0〜数件（上限30／サイクル） |

> **サムネの取得間隔（20秒）は延ばさないこと。** ニコ生側の実際の画像更新は時間帯により変動し、最速で20秒である。効率化は間隔の延長ではなく、二重取得の一本化で行う。

### 7.2 権限

`storage` のみ。追加権限・Service Worker・ホスト権限は不要。

### 7.3 障害時の方針

| 原則 | 内容 |
|---|---|
| 部分失敗を全体失敗にしない | 1番組のカード生成失敗はその番組のみスキップ |
| 取得失敗時は既存表示を維持 | 空リストで画面を消さない |
| 表示は最後まで守る | サムネのCORS失敗時も平文で表示だけ確保 |

---

## 8. 制約・既知の制限

| ID | 内容 |
|---|---|
| L-1 | ニコ生のDOM構造に依存する。ハッシュ付きクラスの部分一致で要素を掴んでおり、視聴ページの改修で壊れやすい |
| L-2 | チャンネル番組にライブサムネは提供されない（ニコ生の仕様）。動かないのは正常 |
| L-3 | 取得の安全上限は500件（`MAX_PAGES=5`）。これを超える番組は表示されない |
| L-4 | 動くサムネのコマ蓄積はニコ生のスクショ更新間隔に依存する（5枚満杯まで数分） |
| L-5 | `cleanup` 後にページが生き残った場合（bfcache 復帰等）、リサイズ追従・動くサムネは復旧しない |
| L-6 | 未着手: `isSidebarLoading()` が定義のみで未使用（死にコード） |

---

## 9. 検証

### 9.1 自動検証

```
npm run verify:loop            # 論理検証＋ソース検査（約1分）
npm run verify:loop -- --real  # 実スケール（間隔60秒）での周期確認（約4分）
npm run verify:e2e             # 実ブラウザ検証（約5分）
```

いずれも **niconico へのログイン不要・実サーバへのアクセスなし**（ページとAPI応答を差し替える）。

### 9.2 検証時の必須手順

> 🔴 **実機で何かを測る前に、必ず拡張を再読み込みすること。**
> `npm run build` で `dist/` を更新しても、Chrome は自動では読み直さない。
> `chrome://extensions` の再読み込み → ページを F5 まで行う。
> **これを怠って古いビルドを測り、誤診した実績がある。**

### 9.3 自動検証できない項目

| 項目 | 理由 | 代替 |
|---|---|---|
| 裏タブでの挙動 | CDP で操作しているページを Chrome は常に `visible` 扱いにする | ソース検査（可視ガードの不在を静的に保証） |
| 実サーバのデータ | ログインが必要 | 手動確認 |

> ⚠️ 「隠れていない状態で取得が続いた」を裏タブ検証の合格としてはならない。前提が成立していない観測は何も確かめたことにならない。

### 9.4 手動確認が必要な範囲

以下は自動検証の対象外であり、実際に使用して確認する必要がある。

- 実サーバの本物のデータでの動作（チャンネル番組・会員限定番組・未ログイン時・通信エラー時）
- 自動移動の一連の流れ
- 動くサムネの見た目
- 複数タブでの設定変更の伝播
- テーマ切替・並び順・幅ドラッグ

---

## 10. 改修時の必読事項

| # | 内容 |
|---|---|
| 1 | **`doc/09-gotchas-and-techdebt.md` を先に読む。** 過去の失敗と、その対策の理由が記録されている |
| 2 | **「対象から外す」変更は、復旧経路も奪っていないか確認する。** 同種の回帰を複数回踏んでいる |
| 3 | **入口ガードだけで満足しない。** 処理中に状態が変わる経路が残る |
| 4 | **意図的な仕様を「バグ」と誤認しない。** 裏タブでリスト取得を続ける／チャンネルのサムネが動かない／サムネ間隔が20秒固定、はいずれも意図的 |
| 5 | **変更後は自己レビューを信用しない。** 観点を分けたレビューと、指摘の反証工程を通すこと |
